{
  "name": "myapp",
  "version": "1.0.0",
  "description": "API web pour TP CI/CD",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --forceExit --testPathPattern=tests/unit",
    "test:e2e": "jest --forceExit --testPathPattern=tests/e2e --testTimeout=15000"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
